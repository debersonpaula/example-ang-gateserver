# ExampleAngGateserver

Example using gateserverauth and Angular 5 + Material.

## Usage for Dev

 - Install dependencies with `npm install`.
 - Run Server with `npm run server`.
 - Run Client with `npm start`.